title ~cluster_size~ nodes
title two clients
title pain on n1

include cockroach.cfg

cast
  n*     play ~cluster_size~ nodes     with p=$((26257+$i)) h=$((8080+$i)) peers=localhost:$((26257+($i+~cluster_size~-1)%~cluster_size~)),localhost:$((26257+($i+~cluster_size~+1)%~cluster_size~))
  kv*    play 2              clients   with port=$((26257+$i)) pprof=$((33330+$i))
  badguy plays               ~nemesis~ with node=n1 p=26257 h=8080 node_id=1
end
