title decommission nemesis
role decommission_nemesis
  :stop     $COCKROACH node decommission $node_id --insecure --port=$p --wait all >deco.log 2>&1
  :restart  $COCKROACH node recommission $node_id --insecure --port=$p >reco.log 2>&1
end
parameter nemesis defaults to decommission_nemesis
