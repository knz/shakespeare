script
  tempo 1s
  action . entails nop
  action s entails :start
  action c entails :prepare; :start; mood blue
  action C entails mood orange; :stop; mood yellow; :restart; mood clear

  prompt every node   s
  prompt every client .c
  prompt badguy       .....C.....
end
